services:
  - type: web
    name: moonlit-tarot
    runtime: node
    plan: free
    buildCommand: echo "No build step"
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
